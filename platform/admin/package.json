{
  "name": "growfi-admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-only admin dashboard for the GrowFi invite queue. Never deploy.",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4101",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4102"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.7.2",
    "vite": "^7.0.0"
  }
}
